#!/usr/bin/env bash

# {{ ansible_managed }}

{% set avacms_venv_bin = avacms_home + "/venvs/" + avacms_service_name + "/bin" %}
{% set executable = avacms_venv_bin + '/gunicorn' %}

source {{ avacms_home }}/avacms_env
{{ executable }} -c {{ avacms_home }}/avacms_gunicorn.py ava_cms.wsgi:application
